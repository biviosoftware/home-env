# -*-sh-*-
if set -o igncr &>/dev/null; then #
    export SHELLOPTS
fi
for f in \
    ~/.pre.bashrc \
    /etc/bashrc \
    ~/src/*/home-env/bashrc.d/*.sh \
    ~/.post.bashrc \
    ; do
    test -f $f && . $f
done
unset f

